"""Layer-wise word representation geometry across transformer architectures,
evaluated on the Codenames Duet clue-giving task.

The model notebooks in ``notebooks/`` import from this package; the
extraction methodology lives here, the per-model orchestration in the
notebooks. See ``contract.py`` for the frozen experimental parameters.
"""

__version__ = "1.0.0"
